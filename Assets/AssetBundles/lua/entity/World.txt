require "core/protobuf"
local Entity = require "entity/Entity"
local World = Class(Entity)

function World:SayHello(msg, name)
	Log("client Say Hello,"..msg..","..name)
end

function World:ServerSayHello(msg, name)
	self:RemoteCall("SayHello", msg, name)
end

return World