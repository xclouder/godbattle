local _M = {}           -- 局部的变量
_M._VERSION = '1.0'     -- 模块版本

local mt = { __index = _M }

function _M:New(self)
    return setmetatable({ }, mt)
end

function _M:Shoot()
	print("Player Shoot")
end

return _M